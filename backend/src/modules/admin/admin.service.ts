

const loginAdmin  = ()=>{
  // validate input
  // check db,
  // check if user is admin - check role must be,
// after credencials are validated, generate jwt token and return to user

};

const getAdminProfile= ()=>{
  // check admin exists in db, if not throw error
  // check if user is admin - check role must be,
  // get user profile from db and return to user 
}

export const adminServices = {
  loginAdmin,
  getAdminProfile
}